---
# Minimal workflow to verify Actions dispatch works in this repository.
name: Actions smoke test

on:
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Actions dispatch OK"
